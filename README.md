This is a Streamlit web app that calculates:

The inverse of a given square matrix

The eigenvalues and eigenvectors of that matrix

Built using Python, NumPy, and Streamlit, this app provides an interactive interface for matrix operations.
